extends layout

block content
  h1= title
  ul
    each message in messages
      li
        h3 #{message.user}
        p #{message.text}
        small #{message.added.toDateString()}
  a(href="/new") Add a new message
